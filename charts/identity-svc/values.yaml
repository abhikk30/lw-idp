image:
  repository: localhost:5001/lw-idp/identity-svc
  tag: dev
  pullPolicy: IfNotPresent

service:
  port: 80
  targetPort: 4001

replicaCount: 2

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

podDisruptionBudget:
  enabled: true
  minAvailable: 1

topologySpreadConstraints:
  maxSkew: 1
  whenUnsatisfiable: ScheduleAnyway

probes:
  liveness:
    path: /healthz
  readiness:
    path: /readyz

extraEnv:
  - name: PG_DSN
    valueFrom:
      secretKeyRef:
        name: identity-svc-pg
        key: PG_DSN
  - name: NATS_URL
    value: "nats://nats.nats-system.svc.cluster.local:4222"
  - name: DEX_ISSUER
    value: "http://dex.dex.svc.cluster.local:5556"
  - name: DEX_AUDIENCE
    value: "lw-idp-gateway"
  - name: DEX_CLIENT_ID
    value: "lw-idp-gateway"
  - name: DEX_CLIENT_SECRET
    valueFrom:
      secretKeyRef:
        name: identity-svc-dex
        key: DEX_CLIENT_SECRET
  - name: GATEWAY_REDIRECT_URI
    value: "http://identity-svc.lw-idp.svc.cluster.local/auth/callback"
  - name: SESSION_SECURE
    value: "false"
  - name: RUN_MIGRATIONS
    value: "1"
  - name: OUTBOX_POLL_MS
    value: "500"
